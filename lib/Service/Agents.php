<?php

declare(strict_types=1);

// This file is auto-generated by oagen. Do not edit.

namespace WorkOS\Service;

use WorkOS\Resource\AgentCredentialValidation;
use WorkOS\Resource\AgentRegistration;
use WorkOS\Resource\ClaimViewResponse;

class Agents
{
    public function __construct(
        private readonly \WorkOS\HttpClient $client,
    ) {
    }

    /**
     * Link a claim attempt to an external user
     *
     * Link an external user to a claim attempt and retrieve the code needed for the agent to complete the claim. The user is looked up by external ID; if no user exists, one is created. When the user belongs to multiple organizations, an explicit organization must be provided.
     * @param string $type The operation to perform on the claim attempt. Currently only `link_external_user` is supported.
     * @param string $claimAttemptToken The token identifying the claim attempt.
     * @param \WorkOS\Resource\AgentAdminLinkClaimAttemptToExternalUserRequestUser $user The user to attach to the claim attempt, identified by email and external ID.
     * @param string|null $organizationId The organization to place the agent in. Required when the user belongs to more than one organization.
     * @return \WorkOS\Resource\ClaimViewResponse
     * @throws \WorkOS\Exception\WorkOSException
     */
    public function updateAttempts(
        string $type,
        string $claimAttemptToken,
        \WorkOS\Resource\AgentAdminLinkClaimAttemptToExternalUserRequestUser $user,
        ?string $organizationId = null,
        ?\WorkOS\RequestOptions $options = null,
    ): \WorkOS\Resource\ClaimViewResponse {
        $body = array_filter([
            'type' => $type,
            'claim_attempt_token' => $claimAttemptToken,
            'user' => $user,
            'organization_id' => $organizationId,
        ], fn ($v) => $v !== null);
        $response = $this->client->request(
            method: 'PATCH',
            path: 'agents/claims/attempts',
            body: $body,
            options: $options,
        );
        return ClaimViewResponse::fromArray($response);
    }

    /**
     * Validate an agent credential
     *
     * Validate an agent credential — an API key or access token — against the environment of the API key used to authenticate the request. This is a read-only check: it never consumes or mutates the credential.
     * @param \WorkOS\Resource\AgentAdminValidateCredentialRequestType $type The kind of credential being validated — an agent API key or an agent access token.
     * @param string $credential The credential value to validate: the API key value for `api_key`, or the access token (JWT) for `access_token`.
     * @param string|null $audience When provided, the access token's `aud` claim is verified against this value. Tokens issued for a different resource are rejected.
     * @return \WorkOS\Resource\AgentCredentialValidation
     * @throws \WorkOS\Exception\WorkOSException
     */
    public function createValidate(
        \WorkOS\Resource\AgentAdminValidateCredentialRequestType $type,
        string $credential,
        ?string $audience = null,
        ?\WorkOS\RequestOptions $options = null,
    ): \WorkOS\Resource\AgentCredentialValidation {
        $body = array_filter([
            'type' => $type->value,
            'credential' => $credential,
            'audience' => $audience,
        ], fn ($v) => $v !== null);
        $response = $this->client->request(
            method: 'POST',
            path: 'agents/credentials/validate',
            body: $body,
            options: $options,
        );
        return AgentCredentialValidation::fromArray($response);
    }

    /**
     * Get an agent registration
     *
     * Retrieve the details of an agent registration by ID. The registration is scoped to the environment of the API key used to authenticate the request.
     * @param string $id The unique ID of the agent registration.
     * @return \WorkOS\Resource\AgentRegistration
     * @throws \WorkOS\Exception\WorkOSException
     */
    public function getRegistration(
        string $id,
        ?\WorkOS\RequestOptions $options = null,
    ): \WorkOS\Resource\AgentRegistration {
        $response = $this->client->request(
            method: 'GET',
            path: 'agents/registrations/' . rawurlencode($id),
            options: $options,
        );
        return AgentRegistration::fromArray($response);
    }
}
