<?php

declare(strict_types=1);

// This file is auto-generated by oagen. Do not edit.

namespace WorkOS\Resource;

readonly class Team implements \JsonSerializable
{
    use JsonSerializableTrait;

    public function __construct(
        /** Distinguishes the team object. */
        public string $object,
        /** Unique identifier of the team. */
        public string $id,
        /** The name of the team. */
        public string $name,
        /** Whether the team can host production environments. `Active` means billing is set up. `Inactive` means a team admin must add a payment method in the WorkOS Dashboard. `Suspended` and `Deleting` mean the team can't be provisioned into. */
        public TeamProductionState $productionState,
        /** The timestamp when production was enabled for the team, or `null` if it never has been. */
        public ?\DateTimeImmutable $productionEnabledAt,
        /** The timestamp when the team was created. */
        public \DateTimeImmutable $createdAt,
        /** The timestamp when the team was last updated. */
        public \DateTimeImmutable $updatedAt,
    ) {
    }

    public static function fromArray(array $data): self
    {
        return new self(
            object: $data['object'] ?? 'team',
            id: $data['id'],
            name: $data['name'],
            productionState: TeamProductionState::from($data['production_state']),
            productionEnabledAt: isset($data['production_enabled_at']) ? new \DateTimeImmutable($data['production_enabled_at']) : null,
            createdAt: new \DateTimeImmutable($data['created_at']),
            updatedAt: new \DateTimeImmutable($data['updated_at']),
        );
    }

    public function toArray(): array
    {
        return [
            'object' => $this->object,
            'id' => $this->id,
            'name' => $this->name,
            'production_state' => $this->productionState->value,
            'production_enabled_at' => $this->productionEnabledAt?->format(\DateTimeInterface::RFC3339_EXTENDED),
            'created_at' => $this->createdAt->format(\DateTimeInterface::RFC3339_EXTENDED),
            'updated_at' => $this->updatedAt->format(\DateTimeInterface::RFC3339_EXTENDED),
        ];
    }
}
