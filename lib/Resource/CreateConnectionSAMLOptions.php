<?php

declare(strict_types=1);

// This file is auto-generated by oagen. Do not edit.

namespace WorkOS\Resource;

readonly class CreateConnectionSAMLOptions implements \JsonSerializable
{
    use JsonSerializableTrait;

    public function __construct(
        /** The Identity Provider metadata URL. When provided, the IdP fields and signing certificates are imported from the metadata document. Mutually exclusive with the manual IdP fields. */
        public ?string $idpMetadataUrl = null,
        /** A custom Assertion Consumer Service (ACS) URL override. When omitted, the standard WorkOS-generated ACS URL is used. */
        public ?string $acsUrl = null,
        /** A custom Service Provider Entity ID (audience) override. When omitted, the connection external key is used. */
        public ?string $spEntityId = null,
        /** The Identity Provider Entity ID. */
        public ?string $idpEntityId = null,
        /** The Identity Provider SSO URL. Required when configuring the connection with manual IdP fields. */
        public ?string $idpSSOUrl = null,
        /**
         * The X.509 certificates used to verify signed SAML responses from the Identity Provider. Required when configuring the connection with manual IdP fields.
         * @var array<string>|null
         */
        public ?array $idpSigningCerts = null,
        /** The customer-owned key pair used to sign SAML requests sent to the Identity Provider. When omitted, WorkOS generates and manages the signing key pair. */
        public ?CreateConnectionKeyPair $spSigningKeyPair = null,
        /**
         * The customer-owned key pairs used to decrypt encrypted SAML responses from the Identity Provider. When omitted, WorkOS generates and manages the encryption key pair.
         * @var array<\WorkOS\Resource\CreateConnectionKeyPair>|null
         */
        public ?array $spEncryptionKeyPairs = null,
    ) {
    }

    public static function fromArray(array $data): self
    {
        return new self(
            idpMetadataUrl: $data['idp_metadata_url'] ?? null,
            acsUrl: $data['acs_url'] ?? null,
            spEntityId: $data['sp_entity_id'] ?? null,
            idpEntityId: $data['idp_entity_id'] ?? null,
            idpSSOUrl: $data['idp_sso_url'] ?? null,
            idpSigningCerts: $data['idp_signing_certs'] ?? null,
            spSigningKeyPair: isset($data['sp_signing_key_pair']) ? CreateConnectionKeyPair::fromArray($data['sp_signing_key_pair']) : null,
            spEncryptionKeyPairs: isset($data['sp_encryption_key_pairs']) ? array_map(fn ($item) => CreateConnectionKeyPair::fromArray($item), $data['sp_encryption_key_pairs']) : null,
        );
    }

    public function toArray(): array
    {
        return [
            'idp_metadata_url' => $this->idpMetadataUrl,
            'acs_url' => $this->acsUrl,
            'sp_entity_id' => $this->spEntityId,
            'idp_entity_id' => $this->idpEntityId,
            'idp_sso_url' => $this->idpSSOUrl,
            'idp_signing_certs' => $this->idpSigningCerts,
            'sp_signing_key_pair' => $this->spSigningKeyPair?->toArray(),
            'sp_encryption_key_pairs' => $this->spEncryptionKeyPairs !== null ? array_map(fn ($item) => $item->toArray(), $this->spEncryptionKeyPairs) : null,
        ];
    }
}
